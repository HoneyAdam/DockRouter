fix: resolve 13 critical code review findings

- Fix dead retry/failover logic with buffered response writer (router.go)
- Fix on-demand TLS to return self-signed fallback during provisioning (manager.go)
- Add mutex to ACME client for thread-safe nonce handling (acme.go)
- Fix XFF header injection by always overwriting with real IP (proxy.go)
- Fix reconciler race condition with consistent lock ordering (reconciler.go)
- Fix Docker event stream goroutine leak with async decode (docker.go)
- Remove unsafe radix tree sync.Pool, use direct allocation (radix.go)
- Fix WebSocket goroutine leak with sync.WaitGroup (websocket.go)
- Fix WebSocket response validation with proper HTTP parsing (websocket.go)
- Route WebSocket through dedicated WebSocketProxy (proxy.go)
- Fix radix tree longest-prefix match to explore all children (radix.go)
- Replace custom intToStr with strconv.Itoa (router.go)
- Fix IPv6 client IP parsing with net.SplitHostPort (proxy.go)